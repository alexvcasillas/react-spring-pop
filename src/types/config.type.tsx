export type fadeConfig = {
  threshold?: number,
  [x: string]: any,
};

export type bounceConfig = {
  threshold?: number,
  tension?: number,
  friction?: number,
  mass?: number,
  [x: string]: any,
};